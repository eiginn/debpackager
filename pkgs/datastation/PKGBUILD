# Maintainer: Ryan Carter <ryan@frobozzelectric.org>
pkgname='datastation'
pkgver=0.11.0
pkgrel=1
arch=('amd64')
depends=()
pkgdesc='App to easily query, script, and visualize data from every database, file, and API.'
license=('Other')
ghrepo='multiprocessio/datastation'
url="https://github.com/${ghrepo}"

source=("https://github.com/${ghrepo}/releases/download/${pkgver}/FILLMEIN")
#noextract=("binary-linux-amd64")
sha256sums=('SKIP')

options=('!strip')

package() {
  #install -Dm 755 "${pkgname}_linux_amd64" "${pkgdir}/usr/bin/${pkgname}"
  #install -Dm 644 "${pkgname}.1" "${pkgdir}/usr/share/man/man1/${pkgname}.1"
  #install -Dm 644 "${pkgname}.completion.zsh" "/usr/share/zsh/vendor-completions/_${pkgname}"
  #install -Dm 644 "${pkgname}.completion.bash" "/usr/share/bash-completion/completions/${pkgname}"
}
