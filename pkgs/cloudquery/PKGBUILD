# Maintainer: Ryan Carter <ryan@frobozzelectric.org>
pkgname='cloudquery'
pkgver=0.23.4
pkgrel=1
arch=('x86_64')
depends=()
pkgdesc='The open-source cloud asset inventory powered by SQL.'
license=('Mozilla Public License 2.0')
ghrepo='cloudquery/cloudquery'
url="https://github.com/${ghrepo}"

source=("https://github.com/${ghrepo}/releases/download/v${pkgver}/cloudquery_Linux_x86_64")
#noextract=("binary-linux-amd64")
sha256sums=('SKIP')

options=('!strip')

package() {
  install -Dm 755 "cloudquery_Linux_x86_64" "${pkgdir}/usr/bin/cloudquery"
}
